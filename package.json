{
  "name": "testtrack-pro",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
      "dev": "concurrently \"cd apps/api && npm run dev\" \"cd apps/web && npm start\""
    
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.8.1"
  }
}
